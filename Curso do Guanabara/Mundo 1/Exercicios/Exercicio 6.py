from math import sqrt
base = float(input('Digite um número: '))
dobro = base * 2
triplo = base * 3
raiz = sqrt(triplo)
print(f'Analisando o número {base}, o seu dobro seria {dobro}, o triplo seria {triplo} e a sua raiz'
      f'quadrada é {raiz:.2f}')
